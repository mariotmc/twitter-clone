import { Tooltip } from "@mui/material";

export const PhoneAlternative = () => {
  return (
    <Tooltip title="Currently unavailable" placement="right" arrow>
      <p id="signup-phone-alternative">Use phone instead</p>
    </Tooltip>
  );
};
